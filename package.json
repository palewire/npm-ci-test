{
  "name": "@palewire/npm-ci-test",
  "version": "0.0.3",
  "dependencies": {
    "bluprint": "^0.4.0"
  },
  "scripts": {
    "build": "rm -rf ./dist/ && mkdir ./dist/ && cp package* ./dist/"
  }
}
